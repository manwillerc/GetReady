import { StyleSheet } from "react-native";
import { useFonts } from 'expo-font';

export const prompt = StyleSheet.create({
    text: {
        fontFamily:'Manrope',
        padding:0,
        marginTop:20,
        fontSize:20
    }
})